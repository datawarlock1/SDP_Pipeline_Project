# SonarQube / SonarCloud

This folder contains the Sonar configuration for the project.

Update `sonar-project.properties` with your SonarCloud organization and project settings.

To run Sonar locally, install the Sonar scanner and run it from the repo root.
