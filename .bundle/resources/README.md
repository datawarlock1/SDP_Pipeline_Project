# Bundle Resources

This directory contains supporting artifacts for the Databricks asset bundle.

- `sample-config.yml` — example pipeline configuration
- `sample-schema.yml` — example schema/resource definition
