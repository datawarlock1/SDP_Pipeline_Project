# Databricks Asset Bundle

This folder contains a minimal Databricks asset bundle for deployment.

- `manifest.json` describes the workspace assets that should be deployed.

Use the bundle in CI or deployment tooling to import notebooks and other assets into Databricks.
